#include <stdarg.h>
#include <stdio.h>
#include <stdlib.h>

void print_all(const char * const format, ...)
{
	va_list ap;
	unsigned int i;
	char *c_hold;

	while (*format != '\0')
	{
		switch(*format) {
		case 'c':
			c_hold
		}

		format++;
	}
}
